---
interface Props { rows: { k: string; v: string | null }[]; }
const { rows } = Astro.props;
---

<dl class="spec">
  {rows.map((row) => (
    <div class="spec__row">
      <dt>{row.k}</dt>
      <dd>{row.v ? row.v : <em>à compléter</em>}</dd>
    </div>
  ))}
</dl>
